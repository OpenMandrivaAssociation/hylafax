--- configure	2007-07-23 09:20:26.000000000 +0200
+++ configure.oden	2007-07-23 09:20:41.000000000 +0200
@@ -3398,11 +3398,6 @@
 	fi
 	;;
     *-*-linux*)
-	if [ "X${DIR_LIB}" != "X/usr/lib" ] && [ "X${DIR_LIB}" != "X/usr/lib64" ]; then
-	    USE_RPATH=' -Wl,-rpath,${LIBDIR}'
-	else
-	    USE_RPATH=''
-	fi
 	DSOSUF=so
 	LIBFAXSERVER='${FAXD}/'$LIBFAXSERVER
 	FAXD='${DEPTH}/faxd'
